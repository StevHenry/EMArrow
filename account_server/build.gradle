sourceCompatibility = 1.15
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
project.ext.mainClassName = "com.uecepi.emarrow.accountserver.EmarrowAccountServer"
eclipse.project.ext.name = appName + "-account_server"

sourceSets.main.java.srcDirs = [ "src/main/" ]
sourceSets.test.java.srcDirs = [ "src/test/" ]

sourceSets.main.resources.srcDirs = ["resources/"]
sourceSets.test.resources.srcDirs = [ "resources/" ]
project.ext.assetsDir = new File("resources")

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

jar {
    manifest {
        attributes 'Main-Class': 'com.uecepi.emarrow.accountserver.EmarrowAccountServer'
    }
}

shadowJar {
    archiveBaseName.set('EmarrowAccountServer')
    archiveClassifier.set('')
}